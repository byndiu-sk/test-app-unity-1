using System.Collections;
using System.Collections.Generic;

public class NitroBar : Bar
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}